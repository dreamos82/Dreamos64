name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: install nasm
      run: sudo apt-get install -y nasm
    - name: make
      run: alias x86_64-elf-gcc=gcc & make
    - name: make tests
      run: make tests
